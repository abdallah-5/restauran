@model ApplicationUser

@if (Model.Image != null)
{
    <li class="nav-item">
        <img class="avatar rounded-circle" src="data:image/*;base64,@(Convert.ToBase64String(Model.Image))" alt="Profile Picture" />
    </li>
}
else
{
    <i class="bi bi-person-fill"></i>
}
Hi! @Model.Name